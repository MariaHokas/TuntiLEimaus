

@{
    ViewBag.Title = "Index";
}

@*<h1 class="w3-center">Päivitä opiskelijan tuntiraporttia</h1>*@ @*/siirretty alas ettei jää rakoa headerin alle*@

<head>
    <title>Leimaus</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <style>

        .abcimage {
            background-image: url("../../Images/opettaja_taustakuva_iso.png");
            /*min-height: 100%;*/
            background-size: cover;
            padding: 30px;
            width: 100%;
            height: 100%;
            background-size: cover;
            background-position: center;
        }

        placeholder
        
        {

            font-size: 10px;
        }

        * {
            box-sizing: border-box;
        }

        form.example input[type=text] {
            padding: 4px;
            font-size: 17px;
            border: 1px solid grey;
            float: left;
            width: 80%;
            background: white;
            border-radius: 4px;
        }

        form.example button {
            float: left;
            width: 20%;
            padding: 4px;
            background: black;
            color: white;
            font-size: 17px;
            border: 1px solid grey;
            border-left: none;
            cursor: pointer;
            border-radius: 4px;
        }

            form.example button:hover {
                background: grey;
            }

        form.example::after {
            content: "";
            clear: both;
            display: table;
        }

        /*.GridviewScrollHeader TH, .GridviewScrollHeader TD {
            padding: 5px;
            font-weight: bold;
            white-space: nowrap;
            border-right: 1px solid #AAAAAA;
            border-bottom: 1px solid #AAAAAA;
            background-color: #EFEFEF;
            text-align: center;
            vertical-align: bottom;
        }

        .GridviewScrollItem TD {
            padding: 5px;
            white-space: nowrap;
            border-right: 1px solid #AAAAAA;
            border-bottom: 1px solid #AAAAAA;
            background-color: #FFFFFF;
        }*/

        .GridviewScrollPager {
            border-top: 1px solid #AAAAAA;
            background-color: white;
        }

            .GridviewScrollPager TD {
                padding-top: 3px;
                font-size: 11px;
                padding-left: 5px;
                padding-right: 5px;
            }

            .GridviewScrollPager A {
                color: white;
            }

            .GridviewScrollPager SPAN {
                font-size: 11px;
                font-weight: bold;
            }

        .inner_table {
            width: 50%;
            border-collapse: collapse;
            overflow: auto;
            height: 300px;
            display: inherit;
        }

        .pg-normal {
            color: white;
            font-weight: normal;
            text-decoration: none;
            cursor: pointer;
        }

        .pg-selected {
            color: white;
            font-weight: bold;
            text-decoration: none;
            cursor: pointer;
            border-radius: 5px;
        }
    </style>
</head>

@section scripts {


    <script type="text/javascript">



        function päivitäTuntiraportti() { //"/opiskelijaid/getlist"
            $.getJSON("/opettaja/getlist", null, function (json) {
                var tuntiraportti = JSON.parse(json);

                var html = "";
                for (var index = 0; index < tuntiraportti.length; index++) {
                    html += "<tr>" +
                        "<td><a href='#'><span class='glyphicon glyphicon-pencil'></span></a></td>" +
                        "<td>" + tuntiraportti[index].LeimausID + "</td>" +
                        "<td>" + tuntiraportti[index].OpiskelijaID + "</td>" +
                        "<td>" + tuntiraportti[index].Etunimi + "</td>" +
                        "<td>" + tuntiraportti[index].Sukunimi + "</td>" +
                        "<td>" + tuntiraportti[index].LuokkahuoneID + "</td>" +
                        "<td>" + tuntiraportti[index].Sisään + "</td>" +
                        "<td>" + tuntiraportti[index].Ulos + "</td>" +
                        "<td><a href='#'><span class='glyphicon glyphicon-trash'></span></a></td>" +
                        "</tr>\r\n";
                    "</tr>\r\n"

                    //Sivunjakaja

                    var pager; // declare the pager variable outside
                    $(function () {
                        pager = new Pager('opiskelijaidList', 10);
                        pager.init();
                        pager.showPageNav('pager', 'pageNavPosition');
                        pager.showPage(1);
                    });

                    function Pager(tableName, itemsPerPage) {
                        this.tableName = tableName;
                        this.itemsPerPage = itemsPerPage;
                        this.currentPage = 1;
                        this.pages = 0;
                        this.inited = false;

                        this.showRecords = function (from, to) {
                            var rows = document.getElementById(tableName).rows;
                            // i starts from 1 to skip table header row
                            for (var i = 1; i < rows.length; i++) {
                                if (i < from || i > to)
                                    rows[i].style.display = 'none';
                                else
                                    rows[i].style.display = '';
                            }
                        }

                        this.showPage = function (pageNumber) {
                            if (!this.inited) {
                                alert("not inited");
                                return;
                            }

                            var oldPageAnchor = document.getElementById('pg' + this.currentPage);
                            oldPageAnchor.className = 'pg-normal';

                            this.currentPage = pageNumber;
                            var newPageAnchor = document.getElementById('pg' + this.currentPage);
                            newPageAnchor.className = 'pg-selected';

                            var from = (pageNumber - 1) * itemsPerPage + 1;
                            var to = from + itemsPerPage - 1;
                            this.showRecords(from, to);
                        }

                        this.prev = function () {
                            if (this.currentPage > 1)
                                this.showPage(this.currentPage - 1);
                        }

                        this.next = function () {
                            if (this.currentPage < this.pages) {
                                this.showPage(this.currentPage + 1);
                            }
                        }

                        this.init = function () {
                            var rows = document.getElementById(tableName).rows;
                            var records = (rows.length - 1);
                            this.pages = Math.ceil(records / itemsPerPage);
                            this.inited = true;
                        }

                        this.showPageNav = function (pagerName, positionId) {
                            if (!this.inited) {
                                alert("not inited");
                                return;
                            }

                            var element = document.getElementById(positionId);

                            var pagerHtml = '<span onclick="' + pagerName + '.prev();" class="pg-normal"> &#171 Edellinen </span> | ';
                            for (var page = 1; page <= this.pages; page++)
                                pagerHtml += '<span id="pg' + page + '" class="pg-normal" onclick="' + pagerName + '.showPage(' + page + ');">' + page + '</span> | ';
                            pagerHtml += '<span onclick="' + pagerName + '.next();" class="pg-normal"> Seuraava &#187;</span>';


                            element.innerHTML = pagerHtml;
                        }
                    }
                }

                $("#opiskelijaidList tbody").html(html);

 

                // tietojen muokkaus
                $(".glyphicon-pencil").click(function () {
                    // etsitään asiakkaan tiedot klikatulta riviltä
                    var LeimausID = $(this).parent().parent().next().text();
                    //alert(opiskelijaID);

                    // haetaan opiskelijan tiedot id:n perusteella //"/opiskelijaid/getsingleopiskelijaid/" korvattu "/opettaja/getsingleopiskelijaid/"
                    $.getJSON("/opettaja/GetSingleTuntiraportti/" + LeimausID, null, function (json2) {
                        var LeimausID = JSON.parse(json2);

                        // kopioidaan tiedot ikkunaan
                        $("#opiskelijaidModal_LeimausID").val(LeimausID.LeimausID);
                        $("#opiskelijaidModal_OpiskelijaID").val(LeimausID.OpiskelijaID);
                        $("#opiskelijaidModal_Etunimi").val(LeimausID.Etunimi);
                        $("#opiskelijaidModal_Sukunimi").val(LeimausID.Sukunimi);
                        $("#opiskelijaidModal_LuokkahuoneID").val(LeimausID.LuokkahuoneID);
                        $("#opiskelijaidModal_Sisään").val(LeimausID.Sisään);
                        $("#opiskelijaidModal_Ulos").val(LeimausID.Ulos);

                        // näytetään ikkuna
                        $("#opiskelijaidModalTitle").text("Muokkaa opiskelijaa");
                        $("#opiskelijaidModalDialog").modal("show");
                    });
                });

                $(".glyphicon-trash").click(function () {
                    // etsitään asiakkaan tiedot klikatulta riviltä
                    console.log("Poistoa klikattu");
                    var LeimausID = $(this).parent().parent().prev().prev().prev().prev().prev().prev().prev().text();
                    var ok = confirm("Haluatko varmasti poistaa tiedon " + LeimausID + "?");

                    if (ok == true) {
                        $.getJSON("/Opettaja/delete/" + LeimausID, null, function (status) {
                            if (status == true) {
                                alert("Tieto on poistettu!");
                                päivitäTuntiraportti();
                            }
                            else {
                                alert("Poistaminen ei onnistunut, yritä uudelleen!");
                            }
                        });
                    }
                });
                
            });
        }
        //päivitäOpiskelijaIDlistaus();
        $(function () {
            päivitäTuntiraportti();
            $("#lisaaUusiTuntiButton").click(function () {

                // Tyhjennetään ikkunan tiedot
                $("#opiskelijaidModal_LeimausID").val("");
                $("#opiskelijaidModal_OpiskelijaID").val("");
                $("#opiskelijaidModal_Etunimi").val("");
                $("#opiskelijaidModal_Sukunimi").val("");
                $("#opiskelijaidModal_LuokkahuoneID").val("");
                $("#opiskelijaidModal_Sisään").val("");
                $("#opiskelijaidModal_Ulos").val("");

                $("#opiskelijaidModalTitle").text("Lisää uudet tunnit opiskelijalle");
                $("#opiskelijaidModalDialog").modal("show");
            });
            //console.log("Alustus valmis!87");

            $("#opiskelijaidModalSaveButton").click(function () {
                // luetaan käyttäjän syöttämät kentät
                var details = {
                    LeimausID: $("#opiskelijaidModal_LeimausID").val(),
                    OpiskelijaID: $("#opiskelijaidModal_OpiskelijaID").val(),
                    Etunimi: $("#opiskelijaidModal_Etunimi").val(),
                    Sukunimi: $("#opiskelijaidModal_Sukunimi").val(),
                    LuokkahuoneID: $("#opiskelijaidModal_LuokkahuoneID").val(),
                    Sisään: $("#opiskelijaidModal_Sisään").val(),
                    Ulos: $("#opiskelijaidModal_Ulos").val()
                };
                console.log("Tiedot luettu");
                $.post("/opettaja/update/", details, function (status) { ///opiskelijaid/update/ korvattu opettaja/update
                    if (status == true) {
                        alert("Uudet tiedot tallennettu!");
                        $("#opiskelijaidModalDialog").modal("hide");
                        päivitäTuntiraportti();
                        //päivitäTuntilistaus();
                        console.log("Tallennus");
                    }
                    else {
                        alert("Tallennus ei onnistunut, yritä uudelleen!");
                        console.log("Tallennus Epäonnistui");
                    }
                });

            });

            päivitäTuntiraportti();
            console.log("Alustus valmis!114");



        });

 

        // search työkalu
        function myFunction() {
            // Declare variables
            var input, filter, table, tr, td, i, txtValue;
            input = document.getElementById("myInput");
            filter = input.value.toUpperCase();
            table = document.getElementById("opiskelijaidList");
            tr = table.getElementsByTagName("tr");

            // Loop through all table rows, and hide those who don't match the search query
            for (i = 0; i < tr.length; i++) {
                td = tr[i].getElementsByTagName("td")[2];
                if (td) {
                    txtValue = td.textContent || td.innerText;
                    if (txtValue.toUpperCase().indexOf(filter) > -1) {
                        tr[i].style.display = "";
                    } else {
                        tr[i].style.display = "none";
                    }
                }
            }
        }


    </script>


}

<div class="w3-content" style="max-width:1400px">
    <div class="w3-container abcimage">


        <h1 style="color:white; margin:60px;" align="center">Päivitä opiskelijan tuntiraporttia</h1>

        <form class="example" style="margin:auto; margin-bottom:15px; float:right; max-width:200px">
            <input type="text" id="myInput" onkeyup="myFunction()" placeholder="Etsi opiskelijanumerolla.." name="search2">
            <button type="submit"><i class="fa fa-search"></i></button>
        </form>


        <Button class="btn btn-default" id="lisaaUusiTuntiButton" style="margin-bottom: 30px">Lisää uusi tuntileimaus</Button>

        

            @*<div style="background-color: black; filter: alpha(opacity=60); opacity:0.6; overflow-x:auto; color: white; filter: alpha(opacity=100); opacity: 1">*@
            <div style="border-collapse: collapse; background: rgba(5,4,2,0.2); color: white; font-size: larger; overflow-x:auto; width: 100%">
                @*<div style="background: rgba(5,4,2,0.2); color: white; font-size: larger; overflow-x:auto;">*@
                <table id="opiskelijaidList" class="table table" cellspacing="0" style="width: 100%; border-collapse: collapse;">
                    <thead>
                        <tr>
                            <th></th>
                            <th>Leimaus</th>
                            <th>OpiskelijaID</th>
                            <th>Etunimi</th>
                            <th>Sukunimi</th>
                            <th>Luokkahuone</th>
                            <th>Sisään</th>
                            <th>Ulos</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody></tbody>

                </table>
            </div>        
            <div id="pageNavPosition">
            </div>
        <div style="color: black;">
            <div class="modal fade" tabindex="-1" role="dialog" id="opiskelijaidModalDialog">
                <div class="modal-dialog" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                            <h4 class="modal-title"><span id="opiskelijaidModalTitle">Muokkaa opiskelijaa</span></h4>
                        </div>
                        <div class="modal-body">
                            <div class="row">
                                <div class="col-md-3">Leimaus ID</div>
                                <div class="col-md-6"><input class="form-control" id="opiskelijaidModal_LeimausID" type="text"></div>
                            </div>
                            <div class="row">
                                <div class="col-md-3">Opiskelijan ID</div>
                                <div class="col-md-6"><input class="form-control" id="opiskelijaidModal_OpiskelijaID" type="text"></div>
                            </div>
                            <div class="row">
                                <div class="col-md-3">Etunimi</div>
                                <div class="col-md-6"><input class="form-control" id="opiskelijaidModal_Etunimi" type="text"></div>
                            </div>
                            <div class="row">
                                <div class="col-md-3">Sukunimi</div>
                                <div class="col-md-6"><input class="form-control" id="opiskelijaidModal_Sukunimi" type="text"></div>
                            </div>
                            <div class="row">
                                <div class="col-md-3">LuokkahuoneID</div>
                                <div class="col-md-6"><input class="form-control" id="opiskelijaidModal_LuokkahuoneID" type="text"></div>
                            </div>
                            <div class="row">
                                <div class="col-md-3">Sisään</div>
                                <div class="col-md-6"><input class="form-control" id="opiskelijaidModal_Sisään" type="text"></div>
                            </div>
                            <div class="row">
                                <div class="col-md-3">Ulos</div>
                                <div class="col-md-6"><input class="form-control" id="opiskelijaidModal_Ulos" type="text"></div>
                            </div>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-default" data-dismiss="modal">Sulje</button>
                            <button type="button" class="btn btn-primary" id="opiskelijaidModalSaveButton">Tallenna</button>
                        </div>
                    </div>

                </div><!-- /.modal-content -->
            </div><!-- /.modal-dialog -->
        </div><!-- /.modal -->
        </div>
    </div>
</div>